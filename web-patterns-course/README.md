
  # Account Form Modal

  This is a code bundle for Account Form Modal. The original project is available at https://www.figma.com/design/J0ltdHgEik2Wq3UalDEt5J/Account-Form-Modal.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  